pipeline {
    agent any

    stages {
        stage("build"){
            steps {
                echo "building the pipeline"
            }
        }
        stage("testing the pipeline") {
            steps {
                echo "testing the pipeline"
            }
        }
        stage("deploying the pipeline") {
            steps {
                echo "deploying the pipeline"
            }
        }
    }
}